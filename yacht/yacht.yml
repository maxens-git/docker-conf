version: '3.3'
services:
    yacht:
        ports:
            - '8500:8000'
        volumes:
            - '/var/run/docker.sock:/var/run/docker.sock'
            - '/docker/yacht:/config'
        image: selfhostedpro/yacht