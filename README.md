# Self hosting

## Config
Copy each `.env.example` to `.env` and fill in your own values before starting a stack.

## Docker compose - npm network
```docker
network_mode: nginx-proxy-manager_default
```

## Services (docker/)
3proxy, adguardhome, adguardhome-sync, ampache, authelia, authentik, backup, beszel, cobalt, code-server, crafty-4, diun, docker-agent, duckdns, duplicati, emby, goaccess, grafana, heimdall, homepage, immich, jellyfin, keycloak, mailcow, metube, minecraft, minidlna, minio, mysql, netdata, nextcloud, nginx-proxy-manager, ntfy, nzbget, php_server, pi-hole, pingvin-share, plex, portainer, poste.io, pyload, roundcube, samba, seedbox, signtool, swag, time-machine, tinyauth, uptime-kuma, vaultwarden, vnc, watchtower, webhook, wireguard, wireguard-easy, wordpress, yacht
